<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.st.dao.TipDao">

<select id="loginUser_tip_count" resultType="int" parameterType="map">
select count(*) from tip where tip_user_id = #{user_id} and tip_state = #{state}
</select>
</mapper>